class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        return true;

         //why 

         //for it to be odd : even - odd 
         //if no odd then its always even
         //if there is one odd then the whole array can be odd 
    }
};